import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
    weatherTitleIcon: {
      width: '40px',
      height: '100%',
      marginRight: '10px',
    },
    root: {
      width: '100%',
      margin: '0 auto',
      maxWidth: '1060px',
    },
  }));
